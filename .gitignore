# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local
.env
.env.*

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Generated by scripts/aggregate.ts — contains real machine data.
# live-data.example.json is the committed sanitized template.
src/data/live-data.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
claude-os

# graphify scratch output (regenerated on ingest)
graphify-out/
# per-project knowledge graph generated locally by graphify ingest — machine
# data, regenerated on demand (index.json + power-design.json stay tracked).
src/data/graphs/claude-os.json

# Stray installer download, not source
Claude Code OS (2.8.1) (1).zip

# Local-only permission allowlist, machine-specific
.claude/settings.local.json
